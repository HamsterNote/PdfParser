---
description: 提交代码更改到 git，包括版本号更新和 CHANGELOG 更新
---

请使用 git-commit-version-manager agent 来处理当前的代码提交。该 agent 将会:
1. 分析当前的代码更改
2. 生成合适的 commit message
3. 更新版本号（如果需要）
4. 更新 CHANGELOG（如果需要）
5. 创建 git commit

请立即启动 git-commit-version-manager agent 来完成这些任务。
